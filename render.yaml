services:
  - type: web
    name: ekt-ai-assistant
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.5
      - key: DEMO_MODE
        value: "1"
      - key: MODEL_NAME
        value: gpt-4.1-mini
      - key: OPENAI_API_KEY
        sync: false
      - key: EKT_API_USER
        value: apiuser
      - key: EKT_API_PASSWORD
        sync: false
